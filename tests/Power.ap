func Power(int n, int power) (int) {
    int total = n;
    for power > 1 {
        total = total * n;
        power = power - 1;
    }
    return total;
}
func main() () {
    int a = Power() <<< 3 <<< 5;
    exit a;
}